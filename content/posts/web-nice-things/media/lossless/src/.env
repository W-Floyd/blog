# Lossless format-comparison unit. make-lossless-comparison.sh writes PNG/WebP/
# AVIF samples whose exact encoding *is* the point, so the normal image passes
# must not re-touch them: only the script runs here.
PROCESS_SCRIPT=true
PROCESS_JPEG=false
PROCESS_PNG=false
